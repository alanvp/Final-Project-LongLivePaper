S3DirectUpload.config do |c|
  c.access_key_id = "AKIAIOA6KDWQOANMJETQ"       # your access key id
  c.secret_access_key = "AKIAIOA6KDWQOANMJETQ"   # your secret access key
  c.bucket = "savepaper"              # your bucket name
  c.region = "oregon"             # region prefix of your bucket url. This is _required_ for the non-default AWS region, eg. "s3-eu-west-1"
  c.url = nil                # S3 API endpoint (optional), eg. "https://#{c.bucket}.s3.amazonaws.com/"
end